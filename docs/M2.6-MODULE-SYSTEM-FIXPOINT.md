# M2.6 — `lib.nixosSystem` infinite-recursion in sui-eval

> Status: **CLOSED** (2026-07-11, session 8).  `lib.nixosSystem { modules =
> []; }` now evaluates end-to-end: `.config.system.name` → `"nixos"` and
> `attrNames sys.options` → 53, both byte-identical to cppnix.  Closed by two
> targeted laziness fixes at their load-bearing cause (ROOT #4a + #4b below) —
> **NOT** the deep mutual-fixpoint re-drive sessions 5–7 projected; the
> `in_promise_eval` null-softening band-aid was **REMOVED** (no longer
> load-bearing).  `sui parity` = 38 match · 0 regressions (35 baseline + 3
> new ROOT-#4 corpus rows); `hello` drvPath
> `j8q5j0x495vfrxgha5m6q40yjdi59334-hello-2.12.2.drv` byte-identical
> throughout; 1324 sui-eval lib + 8 system_eval_parity + 30 diff + 23/6/22/4
> (drv_path/vs_cppnix/construction/property) tests all green;
> `nixos_system_empty_modules_terminates` un-ignored + genuinely passing.
> See the SESSION 8 CLOSURE section at the bottom.
>
> Captured the operator-blocking bug for sui as a drop-in
> `nixos-rebuild`/`darwin-rebuild` engine.  Surfaced by `sui-sweep --corpus
> rebuild` against real NixOS flakes; the M0 shadow-testing surface
> (`sui-spec/specs/rebuild_probes.lisp`) records the probe that failed
> (`nixos-toplevel-outpath`, `nixos-dry-run-closure`,
> `nixos-closure-references`).

## Progress log

- **2026-07-11 — dynamic-inner-attrpath-key laziness (ROOT #1, byte-verified, sealed).**
  The `InfiniteRecursion` this doc opened on is gone (the `Promise` variant
  landed); the frontier moved to `concatLists: expected list, got null` in
  `lib/modules.nix`. Root-caused via a force-stack capture: forcing the
  `config` fixpoint (`mapAttrsRecursiveCond … options`) forces module
  collection (`doCollect`/`rawDefinitionsByName`), which forces each
  module's `config` attrset to WHNF, which **eagerly forced the inner
  dynamic key** in `config.homes.${cfg.userName} = 7` → re-read `config`
  mid-force → empty-Promise partial → the M2.6 softening returned `null` →
  the definition routed to `homes.<null>` instead of `homes.<name>` (empty
  result / `concatLists null`). The pure-builtins reduction (no module
  system):

  ```sh
  # nix → 9 ; sui (pre-fix) → throws "I"
  nix  eval --impure --expr 'let s = { a.${throw "I"} = 7; other = 9; }; in s.other'
  ```

  cppnix defers a dynamic key that is NOT at the *head* of an attrpath:
  `{ a.${e} = v; }` builds `{ a = <thunk {${e}=v}>; }`, so `e` forces only
  when `.a` is demanded — proven by `nix eval '{ a.${throw}=…; other=9; }.other'`
  → `9`. sui forced the whole attrpath eagerly at construction. **Fix:**
  `build_deferred_tail_attr` in `sui-eval/src/eval.rs` — only the head key
  is eager; a lone dynamic tail becomes a deferred `Native` thunk that
  evaluates the tail keys (dynamic included) at head-demand time; a
  dynamic-tail *collision* under one head stays eager (static deep-merge
  preserved). Typescaped as `AttrKeyForceSite` (`defattrkey-forcesite`) in
  the `laziness` domain; sealed as two `sui parity` corpus rows (30 match).
  Regression tests: `dynamic_inner_attr_key_*` in `sui-eval::eval::tests`.

- **ROOT #2 — CLOSED 2026-07-11 (the OVER-FORCE; byte-verified).** The
  load-bearing root was *not* the re-entry softening below; it was that
  `build_tail_attrs_now` recursed a MULTI-LEVEL deferred tail eagerly.
  For `config.homes.${e} = 7`, forcing the head `.config` (or merely
  reading its `._type`, as `pushDownProperties m.config` does in the
  module system's definition collection) resolved the WHOLE tail,
  forcing `${e}` — the sibling `homes` definition's dynamic key that
  cppnix never touches when only `config.pleme.userName` is selected.
  cppnix desugars `a.b.c = v` into per-level lazy nested attrset
  literals, so forcing one level yields `{ next = <thunk> }` and stops.
  The fix makes `build_tail_attrs_now` resolve exactly ONE tail level and
  re-defer the rest (`sui-eval/src/eval.rs`). Minimal repro →
  `{"drzzln":7}`; KEYFORCE discriminator → `"drzzln"`; `sui parity` = 33
  match, 0 regressions (`hello` drvPath `8550ijfw…-hello-2.12.2.drv`
  byte-identical). Typescaped as the `nested-tail-dynamic`
  `AttrKeyForceSite` (`HeadDemand` is now PER-LEVEL) in the `laziness`
  domain; sealed as three new `sui parity` corpus rows (`multi-level
  dynamic-tail attrpath — {middle-level read, sibling under head, leaf
  demand}`). Regression tests: `module_dynamic_key_from_sibling_option_
  resolves` + `module_unrelated_select_does_not_force_sibling_dynamic_key`
  in `sui-eval::tests::diff_eval_nixpkgs_lib`. NOTE: the full
  `lib.nixosSystem { modules = []; }` still diverges past ROOT #2 (now a
  distinct `TypeMismatch set/null` frontier), so
  `nixos_system_empty_modules_terminates` stays `#[ignore]`.

- **ROOT #3 — CLOSED 2026-07-11 (the DYNAMIC-KEY classifier + collision
  OVER-FORCE; byte-verified).** Same family as ROOT #1/#2 (a dynamic
  attrpath key forced eagerly at construction → a `config.<x>` read while
  `config` is a mid-fixpoint empty-Promise partial → the `set/null`
  softening), in TWO sub-cases the ROOT #2 machinery missed:
  - **3a — interpolated-string tail key.** `attrs_have_dynamic` matched
    only bare `ast::Attr::Dynamic` (`${e}`); an INTERPOLATED-STRING key
    (`config.environment.etc."iwd/${configFile.name}"`, where `configFile`
    reads `with config.networking.networkmanager`) is `ast::Attr::Str`,
    so it fell to the eager path. Fix: `attr_is_dynamic` now treats a
    `Str` attr with ≥1 `InterpolPart::Interpolation` as dynamic (a
    purely-literal `"foo"` stays static), routing it through the ROOT #1/#2
    per-level deferral.
  - **3b — dynamic tail key under a COLLIDING head.** The deferral bailed
    when a sibling binding already wrote the head
    (osquery's `systemd.services.… = …` then
    `systemd.tmpfiles.settings."10-osquery".${dirname …}.d`), so the eager
    path forced the dynamic key. Fix: `merge_deferred_dynamic_tail`
    descends the existing head along the tail's STATIC prefix and splices
    a deferred thunk at the first dynamic level — preserving BOTH laziness
    and the static deep-merge with the sibling.

  Minimal pure-lib repros (both `lib.evalModules [modA modB]`, iterate in
  ms, no `getFlake`) → both return `42` matching nix:
  `config.environment.etc."iwd/${nm}"` (3a) and
  `config.sd.tmpfiles.${dirOf config.svc.path}.d` under a colliding
  `config.sd.services.x` (3b). `sui parity` = **35 match · 0 tracked · 0
  regressions** (`hello` drvPath `j8q5j0x495vfrxgha5m6q40yjdi59334-hello-
  2.12.2.drv`, byte-identical throughout); `sui-eval` lib **1319 passed**;
  `sui-spec` **457 passed** (single-threaded; the 2 parallel-flaky
  fetchurl/NAR tests are unrelated). Typescaped as the
  `interpolated-tail-dynamic` + `colliding-head-tail-dynamic`
  `AttrKeyForceSite` rows (both `Tail → HeadDemand`) in the `laziness`
  domain; sealed as two new `sui parity` corpus rows (`dynamic tail under
  colliding head — {static sibling read stays lazy, dynamic branch
  resolves}`). Regression tests:
  `module_interpolated_string_dynamic_key_stays_lazy`,
  `module_dynamic_tail_key_under_colliding_head_stays_lazy`,
  `module_dynamic_tail_collision_preserves_static_sibling` in
  `diff_eval_nixpkgs_lib` + five pure-unit tests
  (`interpolated_string_attr_key_*`, `purely_literal_string_attr_key_*`,
  `dynamic_tail_key_under_colliding_head_*`) in `sui-eval::eval::tests`.

  **ROOT #4 is now EXPOSED (not this one).** The full
  `lib.nixosSystem { modules = []; }` advances PAST ROOT #3 (`set/null`)
  to a DISTINCT `TypeError("expected list, got null")` at `concatLists`
  (force-stack: `checkUnmatched → seq → mergeModules'` recursion through
  `<native-thunk>` frames + `mkAliasDefinitions`/`atDepth`). *(Refined
  2026-07-11: the `doRename`/`atDepth` is only the CARRIER; ROOT #4's true
  cause is the **field-lazy fixpoint softening** — a `config.services.<x>`
  read mid-fixpoint softens to `null` — NOT the alias itself. See the ROOT
  #4 entry below.)* ROOT #4 is a new root and
  `nixos_system_empty_modules_terminates` stays `#[ignore]`.

- **ROOT #4 — OPEN 2026-07-11 (mechanism byte-verified; the field-lazy
  fixpoint, NOT a dynamic-key over-force; no safe narrow fix — no
  parity-regressing hack shipped).** The full `lib.nixosSystem { modules =
  []; }` advances past ROOT #3 and dies at `concatLists: expected list, got
  null` (`checked = seq checkUnmatched` at `lib/modules.nix:337` →
  `merged.unmatchedDefns` at line 900 → a deep `mergeModules'` recursion).
  **Discriminator (byte-verified):** `SUI_M26_HARDSOFTEN=services` changes the
  outcome → the failing path is fed by a `config.services.borgbackup`
  select-miss softening (`missing-key=services` in `borgbackup.nix` — i.e.
  `config.services.borgbackup` read while `config` is the empty-`Promise`
  partial). borgbackup's `config = mkIf (with config.services.borgbackup; …)
  (with config.services.borgbackup; { users = mkMerge (mapAttrsToList
  mkUsersConfig repos); … })`; `pushDownProperties` (`modules.nix:1282`)
  forces the `mkIf` content UNCONDITIONALLY to enumerate its keys (cppnix does
  the same), forcing `with config.services.borgbackup`. **cppnix** resolves
  `config`'s `services` key independently to the option default `{}` → `repos
  = {}` → `mkMerge []` → `[]`. **sui** re-enters the mid-force monolithic
  `config` `Promise` → empty partial → the `in_promise_eval` softening returns
  `null` → `repos = null` → `mapAttrsToList mkUsersConfig null` = `null` →
  `mkMerge null` → `concatLists null`.

  This is the **field-independence gap** (softening returns `null` where cppnix
  resolves the field to its real default), the deep field-lazy fixpoint issue
  the STATUS section below already names — **not** a dynamic-attrpath-key
  over-force (ROOT #1/#2/#3, all sealed). The `doRename`/`atDepth`
  (`setAttrByPath`'s `{ ${elemAt attrPath n} = … }` dynamic HEAD key) on the
  force chain is only the CARRIER; its key force is correct (cppnix forces a
  dynamic head key too). **No safe narrow over-force to kill was found:**
  `pushDownProperties` forcing `mkIf` content and `atDepth` forcing its head
  key are both cppnix-faithful. **No minimal pure-lib reducer exists** — the
  empty-`Promise` partial arises only from the FULL base-module `_module.args`
  fixpoint re-entrance (bisection data points I/J). The pure-lib shapes that
  CARRY the bug in-situ (`mkRenamedOptionModule`; `doRename` into `attrsOf
  submodule`; `mkIf (config.svc != {}) (with config.svc; …)` over `attrsOf
  submodule`) all resolve CORRECTLY in isolation (sui == nix) — pinned as
  boundary regression tests `module_rename_forwards_definition`,
  `module_rename_into_submodule_unrelated_select`,
  `module_mkif_config_read_over_attrs_of_submodule` in `diff_eval_nixpkgs_lib`.
  **Full repro + mechanism:** `docs/repros/root4-concatlists-null.md`.
  **Diagnostics landed** (byte-neutral, env-gated, zero hot-path cost):
  `SUI_M26_CLTRACE` (non-list `concatLists` element type + file stack),
  `SUI_M26_CLDUMP` (+`SUI_TRACE_EVAL=ring` → ring-buffer tail at the null),
  `SUI_M26_HARDSOFTEN=<substr>` (throw at the softening whose demanded attrpath
  contains `<substr>` — the discriminator that pinned `services.borgbackup`),
  `SUI_M26_MAXFRAMES=<n>` (configurable force-stack depth). `sui parity` held
  at **35 match · 0 regressions** (`hello` drvPath
  `8550ijfw8imvqnky30gwdvwlidq3g42b-hello-2.12.2.drv`, byte-identical) through
  every experiment. `nixos_system_empty_modules_terminates` stays `#[ignore]`;
  ROOT #4 is not closed and no ROOT #5 is exposed (evaluation still stops at
  the ROOT #4 `concatLists null`). **The genuine fix is the deep field-lazy
  `ThunkRepr::Promise` re-drive** (resolve the demanded field against the real
  merge lattice, not an empty sentinel) — the STATUS section's mechanism (a),
  explicitly deferred as byte-parity-risky and warranting a deliberate design
  pass, not a rushed edit.

- **HISTORICAL frontier (ROOT #2, superseded by the OVER-FORCE fix above):
  the routing-re-entry softening.**
  With ROOT #1 fixed, `config.pleme.userName` now resolves correctly in
  isolation, but demanding `config.homes` still re-enters `config` *during
  its own force* (module collection → `homes` option merge → the deferred
  tail thunk forces `${cfg.userName}` = `config.pleme.userName` while the
  `config`/`options` fixpoint is mid-force). At that point the empty-Promise
  partial + the M2.6 null-softening (`eval_select` under `in_promise_eval`)
  return `null`, so `homes.${…}` still routes to `homes.<null>` → `{}`
  where nix has `{"<name>":7}`. Minimal general repro (pure nixpkgs lib):

  ```sh
  # nix → {"drzzln":7} ; sui → {}
  sui eval --no-vm --impure --json --expr '
    let lib = import <nixpkgs/lib>;
      modA = { config, lib, ... }: {
        options.pleme.userName = lib.mkOption { type = lib.types.str; default = "luis"; };
        options.homes = lib.mkOption { type = lib.types.attrsOf lib.types.int; default = {}; };
        config.homes.${config.pleme.userName} = 7;
      };
      modB = { ... }: { config.pleme.userName = "drzzln"; };
    in (lib.evalModules { modules = [ modA modB ]; }).config.homes'
  ```

  This is the *field-independence* gap: cppnix resolves `config.pleme.userName`
  during `config`'s own force because `config`/`options`/`matchedOptions` are
  lazy attrsets whose fields resolve independently (the `pleme.userName`
  option merge depends only on modB's constant, not on `homes`). sui's
  `config` is one monolithic Promise thunk whose re-entry yields the *empty*
  partial. The genuine fix (no band-aid) is field-lazy fixpoint resolution
  across the `config ↔ options ↔ collection` cycle — i.e. the Promise cell
  must resolve the demanded field against the real merge lattice, not an
  empty sentinel. This is a deep, multi-root change; the `sui-spec`
  `module_compiler`/`module_solver` typed path is the typescaped destination.
  The `SUI_BLACKHOLE_AS_NULL` / `in_promise_eval` null-softening is the
  documented band-aid to *remove* once field-lazy resolution lands.

## TL;DR

`lib.evalModules` from nixpkgs (`lib/modules.nix`) works in sui-eval
for synthetic minimal modules.  `lib.nixosSystem`, which calls the
same `evalModules` with the full NixOS module set, hits infinite
recursion through `extraArgs ↔ matchedOptions` even when the
operator passes `modules = []`.

## Minimal repro

```sh
sui eval --no-vm --impure --raw '
let nixpkgs = builtins.getFlake "github:NixOS/nixpkgs/b77b3de";
in (nixpkgs.lib.nixosSystem { system = "x86_64-linux"; modules = []; })
   .config.system.name
'
```

Expected: a short string like `"nixos"`.
Today: `Eval(InfiniteRecursion(...))`.

## Force chain (16 frames)

Captured with the diagnostic improvement in commit-following-this-doc
(force-stack always populated, not gated on `SUI_TRACE_EVAL`):

```
→ let \n\n          # For definitions that have an associated opt    (lib/modules.nix)
→ mapAttrsRecursiveCond (v: !isOption v) (_: v: v.value) options     (lib/modules.nix)
→ mapAttrs (n: v: v.matchedOptions) resultsByName                    (lib/modules.nix)
→ mapAttrs (name: decls: # We're descending …)                       (lib/modules.nix)
→ zipAttrsWith (n: v: v) (map (module: …))                           (lib/modules.nix)
→ (reverseList (doCollect { }).modules)                              (lib/modules.nix)
→ length xs                                                          (lib/lists.nix)
→ filterModules modulesPath (collectStructuredModules unknownM…)     (lib/modules.nix)
→ keyFilter modules                                                  (lib/modules.nix)
→ concatMap ({ file, disabled }: map (moduleKey file) disabled)      (lib/modules.nix)
→ concatLists (catAttrs "disabled" modules)                          (lib/modules.nix)
→ (if module.disabledModules != [] then …)                           (lib/modules.nix)
→ checkModule (loadModule args parentFile "${parentKey}:anon-$")     (lib/modules.nix)
→ (loadModule args parentFile "${parentKey}:anon-${toString n}…")    (lib/modules.nix)
→ (applyModuleArgsIfFunction (toString m) (import …))                (lib/modules.nix)
→ <native-thunk>                                                     (<eval>)
```

The cycle bottoms out at a sui-internal `Native` thunk — i.e. a
`ThunkRepr::Native(f)` created by sui-eval (not parsed from Nix
source).  This thunk is the `extraArgs` value that
`applyModuleArgsIfFunction` is forcing to pass as the second argument
to each module function (`f args`).  The chain re-enters the top
because `extraArgs.<x>` depends on `config.<y>` which depends on the
module set's `options`, and the module set's options depend on
`extraArgs` having been resolved.

## Why minimal `evalModules` works but `nixosSystem` doesn't

The synthetic `lib.evalModules { modules = [...]; }` call we used as
a regression test passes a `modules` list whose attrs declare options
+ config DIRECTLY, with no `_module.args` dependency.  Sui-eval's
fix-point + blackhole detection handles that case correctly (see
`force_chain_captures_self_reference` and friends in `trace.rs`
tests).

`lib.nixosSystem` is structurally different: it bootstraps an
attrset of `extraArgs` (`{ inherit lib config options pkgs modulesPath …; }`)
where every field is a closure over the not-yet-computed module
result.  cppnix breaks this cycle by making each entry a SEPARATE
lazy thunk and forcing them only on first deep access; in particular,
`_module.args.pkgs` only forces when a module body actually selects
it.  Sui-eval today appears to eagerly force the full `extraArgs`
attrset somewhere in the call chain — probably in `args // extraArgs`
or in the `mapAttrs (name: _: addErrorContext context)` wrapping —
which collapses the fix-point.

## Suggested investigation paths

1. **Instrument `Native` thunk creation sites** — print a backtrace
   when sui-eval creates a `ThunkRepr::Native`.  The chain shows the
   cycle bottoms out at one of these; pinpoint *which* native-thunk
   construction is the bootstrap of `extraArgs`.

2. **Check `//` (attrset merge) operator** — `args // extraArgs` is
   the immediate predecessor in the chain.  If sui's `//` forces both
   operands' structure (key sets), it will demand-force a thunked
   `extraArgs` even when only `args`'s keys would suffice.  cppnix's
   `//` lazily merges; the result is an attrset whose KEY lookup
   defers forcing the source's individual values.

3. **Trace `_module.args` resolution** — that's the option whose
   value is the fix-point seed.  In nixpkgs `lib/modules.nix`,
   the `_module.args` declaration uses `lazyAttrsOf` with a custom
   merge function that's `lazy-aware`.  Sui's option-type registry
   (`sui-spec/src/module_system.rs::MergeStrategy`) lists `AttrsetMerge`
   for `attrs`, which deep-merges — is the LAZY-attrs variant
   correctly preserved?

4. **Compare against `builtins.sui.evalModules`** — sui's typed-Lisp
   bridge in `sui-eval/src/builtins/module_system_bridge.rs` already
   has an implementation of the algorithm.  If that bridge avoids the
   recursion (because it runs the fixed-point in Rust), there's an
   incremental-cutover path: detect `lib.evalModules` calls and
   redirect to the bridge.

## Operator workaround until fixed

Don't enable `services.sui-as-nix.enable = true;` on the host yet.
The infrastructure is in place (`pleme-io/sui` flake exposes
`packages.<system>.sui-as-nix` + `nixosModules.default-as-nix`); the
operator flip is the last step after this M2.6 ratchet lands.  In the
meantime, sui-as-nix can still be the user-PATH shim for everyday
`nix eval` / `nix build` / `nix flake show` — only the system-rebuild
path requires the M2.6 fix.

Sweep should remain Match-clean for the operator's flake before the
cutover:

```sh
sui-sweep --sui $(which sui) --corpus rebuild ~/path/to/flake
```

## Second attempt — `SUI_BLACKHOLE_AS_NULL` bridge (env-gated, lands)

Tried: in the `ThunkRepr::Blackhole` arm of `force_inner`, return
`Value::Null` instead of `EvalError::InfiniteRecursion` when the
operator sets `SUI_BLACKHOLE_AS_NULL=1`.  Theory: cppnix exposes the
not-yet-complete value on re-entrance; we can't synthesize that
without a proper "partial promise" thunk variant, but a null
sentinel combined with `Select`'s NotAttrs-falls-to-default
behavior approximates it enough to let evaluation proceed.

Result: **the cycle breaks**.  Sui no longer raises
`InfiniteRecursion` on `lib.nixosSystem { modules = []; }`.  The
evaluation continues through `modules.nix`,
`system/boot/networkd.nix`, and bottoms out at:

```
TypeError("type error: cannot select from null
  while calling function defined in modules.nix
  while calling function defined in system/boot/networkd.nix
  …  while calling the 'concatLists' builtin
  while calling the 'concatMap<partial>' builtin
  while calling the 'elem<partial>' builtin
  while calling the 'genericClosure' builtin
  …")
```

This is downstream: some `nixos` module reads `x.y` where `x` is
the null sentinel (returned by the blackhole bridge), and the
access doesn't have `or default`.  cppnix never reaches this state
because cppnix's `x` would have been the not-yet-complete attrset,
which CAN be `.y`-selected (recurses lazily).

The bridge is shipped behind the env var (default OFF) so operators
can experiment.  Default-off because masking re-entrance as null
silently hides legitimate user-code bugs (`let r = r; in r.x` would
return null instead of erroring).

## Genuine fix: `ThunkRepr::Promise(Rc<RefCell<NixAttrs>>)`

The empirical bridge above pinpoints the missing piece.  The proper
fix is a new `ThunkRepr` variant carrying a `RefCell` of partially-
constructed `NixAttrs`:

* Outer `let x = f x; in x` (or any self-recursive thunk) detected
  at `eval_let` / `eval_letrec` time — flag the binding.
* When forcing such a thunk, transition `Suspended → Promise(empty)`
  (not `Suspended → Blackhole`).  Run the body.
* As the body evaluates attrset construction, append keys to the
  promise's `NixAttrs` incrementally.
* Inner forces that re-enter the still-evaluating thunk return the
  current state of the promise (an actual `Value::Attrs`, supporting
  `.key` selection, but missing keys still trigger the cycle).
* When the body returns, transition `Promise → Evaluated(final)`.

This matches cppnix's semantics for `lib.fix` / `let x = f x; in x`
and unblocks the rest of `lib.modules.nix` without any sentinel
hacks downstream.  The Promise variant interacts with `!Send` /
`!Sync` correctly (single-threaded evaluator, `Rc<RefCell<>>` for
the cell) and the existing blackhole detection still triggers for
non-attrset-shaped cycles (e.g. `let r = r; in r + 1` still errors
because `Promise` only makes sense for attrset-typed fix points).

## What the partial fix tried (and what it tells us)

Attempted: catch `EvalError::InfiniteRecursion` in `eval_select`
(both at `force_concrete` of the base and at `traverse_attrpath`)
and fall back to the `or default` branch.  Hypothesis: the cycle
flows through `config._module.args.<x> or args.<x>` in modules.nix's
`applyModuleArgsIfFunction`, so catching there would unblock.

Result: **zero hits** during nixosSystem evaluation.  Verified with
an `SUI_M26_TRACE=1` instrument that logged every catch — the
nixosSystem failing path never triggers `eval_select`'s InfRec
fallback.  Conclusion: the cycle bottoms at a sui-internal Native
thunk and re-enters the OUTER let-binding thunk (`declaredConfig`)
through a path that does NOT pass through a `Select-with-default`.
The catch is still landed because it's a correctness improvement
for the unrelated `let x = x; in x.foo or default` pattern (passes
that microtest) — but it is not the M2.6 fix.

The genuine fix is at the Native-thunk re-entrance: when
`mapAttrs` / `zipAttrsWith` / `lib.fix`-driven thunks re-force the
outer fix-point thunk that is currently being computed, sui must
provide the PARTIAL accumulated value (cppnix's behavior for
`let x = f x; in x` re-references), not blackhole with
InfiniteRecursion.  That requires extending the thunk state
machine with a "promise" variant that carries a not-yet-complete
attrset growing as `f`'s body runs.

## Bisection results (what passes vs. what fails)

The cycle is **not** present in any of the building blocks; it
emerges only from the full base-module-set interaction.  Concrete
data points (`sui eval --no-vm --impure --raw …`, nixpkgs pinned to
`b77b3de`):

| # | scenario | sui result |
|---|---|---|
| A | `lib.evalModules { modules = [ { config = {}; } ]; }` → `.config.foo or "ok"` | **`"ok"`** ✓ |
| B | `_module.args = { x = "lazy"; }` then read `_module.args.x` | **`"lazy"`** ✓ |
| C | one module sets `_module.args.greeting`, another destructures `{ greeting, ... }` | **`"hello"`** ✓ |
| E | `specialArgs = { extra = "special"; }` flow to destructured arg | **`"special"`** ✓ |
| F | `eval-config.nix { system; modules = []; baseModules = []; }` | bounded error (missing option) — no infinite recursion |
| H | `baseModules = [ "${nixpkgs}/nixos/modules/misc/version.nix" ]` | bounded error (missing `assertions`) — no infinite recursion |
| I | `baseModules = [ assertions.nix, version.nix ]` (no nixpkgs.nix) | bounded error (missing `environment`) — no infinite recursion |
| J | `baseModules = [ nixpkgs.nix ]` alone | `AttrNotFound("_module")` — no infinite recursion |
| K | `lib.nixosSystem { system; modules = []; }` (baseModules defaults to the full 1982-module list) | **InfiniteRecursion (16-frame chain above)** ✗ |

So:
- the bug requires the FULL baseline-module set
- single modules and small subsets do not reproduce it
- the destructure pattern + `_module.args` extension + specialArgs
  flow are all individually correct in sui-eval
- a strong suspect is `nixos/modules/misc/nixpkgs.nix:358`:
  ```nix
  _module.args = {
    pkgs = lib.mkOverride lib.modules.defaultOverridePriority finalPkgs;
  };
  ```
  where `finalPkgs` is a derivation of `config.nixpkgs.*`.  The
  cppnix merge system resolves priority on the `mkOverride` wrapper
  WITHOUT forcing `content` (`finalPkgs`).  If sui's option merger
  forces `content` while resolving priority for `_module.args.pkgs`,
  the cycle is hit: `finalPkgs` reads `config`, `config` is being
  computed, blackhole.

## Operational repro on rio

```sh
$SUI eval --no-vm --impure --raw '
  let n = builtins.getFlake "github:NixOS/nixpkgs/b77b3de";
  in (n.lib.nixosSystem { system = "x86_64-linux"; modules = []; })
     .config.system.name
'
# → Eval(InfiniteRecursion("force chain (16 frames): …"))
```

Note: this uses `--no-vm` (tree-walker).  The bytecode VM hits the
same blackhole and falls back to the tree-walker, so the cycle is in
the shared module-system / option-merge code path, not in either
engine specifically.

## Regression test

A failing test pinning the minimal nixosSystem repro will land
alongside this doc.  It is marked `#[ignore]` until M2.6 closes.
(Shipped: `sui-eval/tests/system_eval_parity.rs::nixos_system_empty_modules_terminates`.)

---

## STATUS 2026-07-11 — root precisely pinned; supersedes the sections above

> Everything above this line is the historical trail (the original
> `InfiniteRecursion` framing + the `Promise` / sentinel attempts).
> **The `InfiniteRecursion` is GONE** — the landed `Promise` machinery
> broke the cycle.  Two deep byte-parity-guarded efforts + a `mapAttrs`
> laziness check converged on the true root.  This section is the
> de-risked implementation spec for the next attempt.  The byte-parity
> floor (`sui parity` = 28 match, `hello` drvPath match) held through
> every experiment below; nothing here regressed it.

### The failure today (not a cycle — a wrong partial)

`lib.nixosSystem { modules = []; }` → `.config.system.name` no longer
raises `InfiniteRecursion`.  It bottoms out at a **wrong-partial type
error** — with the empty-attrs sentinel it advances all the way into
option-value merging (`mergeDefinitions` / `cfg.content` /
`setAttrByPath`) and dies at `cannot coerce set to string`.

### The confirmed root — a monolithic recursive thunk, not a merge bug

- The re-entrant miss is always **`config._module.args.${name}`**
  (`name` ∈ `pkgs`, `systemdUtils`, …), where the base is the empty
  `Promise` partial.
- `config` is defined as `mapAttrsRecursiveCond (v: !isOption v)
  (_: v: v.value) options`.  Forcing `config._module.args.pkgs` resolves
  the option value → `nixpkgs.nix:358` `_module.args.pkgs = mkOverride …
  finalPkgs` → `finalPkgs` reads **`config.nixpkgs.*`**, a *different*
  key.  In cppnix this terminates: `config` is a **lazy attrset value**
  whose key-structure is published to WHNF *without* forcing the leaf
  `.value` thunks, so `config.nixpkgs` is an independent key thunk — no
  re-entrance.
- In sui, `config` is **one recursive-let binding thunk** whose body is
  computed to WHNF in a single `evaluator(expr, env)` call.  The
  self-reference (`config.nixpkgs` via the `_module.args.pkgs → finalPkgs`
  chain) is demanded **during that body's own WHNF computation, before
  WHNF is reached**, so it re-enters the same still-computing thunk and
  gets the empty `Promise` partial.  The `config`/`options`/`merged`
  fixpoint thunks are `recursive = false` (`is_self_recursive_binding`
  misses them — their RHS never textually self-names) and reach `Promise`
  only via the mid-body `Blackhole → Promise` promotion.

### Ruled out (do not re-attempt these)

1. **Any single-type sentinel.** `null` dies at strict `set`/`bool`
   sites; empty-attrs advances further but dies at a strict `string`
   site.  No one concrete type satisfies every downstream position.
   *(proven)*
2. **`mkOverride`/priority over-force in a sui merger.** The merge is
   pure interpreted nixpkgs Nix here, not a sui-native merger. *(ruled
   out by direct read)*
3. **`mapAttrs` eagerness.** `builtins.mapAttrs` (`sui-eval/src/builtins/
   attrs.rs:76`) is already lazy per value (`Thunk::new_native` per key);
   it is *not* the over-eager site. *(checked 2026-07-11)*
4. **Per-key lazy view via re-driving the atomic body to WHNF.** Built,
   instrumented, proven to reproduce the same empty/error (the body's
   WHNF is *not* reached before the re-entrant demand — `config`'s body
   is an `if`-guard whose evaluation *is* what triggers the demand), and
   fully reverted. *(proven — `value.rs` is byte-identical to base)*
5. **Redirect `lib.evalModules` → `module_system_bridge.rs`.** The bridge
   is an M3.1 toy (`{options,config,imports}` only; no `mkOverride`/
   `mkMerge`/priorities/`submoduleWith`/`freeformType`); driving real
   `lib.evalModules` through it is a full module-system re-implementation
   with fleet-wide parity risk. *(ruled out)*

### The genuine fix (mechanism (a)) — an evaluator laziness-model change

Make **computed recursive bindings publish their result attrset's
per-key structure (keys as thunks) to the fixpoint cell as they reach
WHNF, before the strict downstream re-enters** — i.e. give a computed
(non-literal) recursive attrset the same lazy-structure property that
`eval_attrset`'s `is_rec` branch already gives a `rec { … }` *literal*
(keys inserted as thunks before values force).  Then a re-entrant
`config.nixpkgs` during `config`'s construction resolves against the
already-published key thunk instead of an empty sentinel — exactly
cppnix's `config` semantics.

This is a **force-ordering / attrset-publication change in `eval.rs`**
(how an `if`/`let`/`select`-bodied recursive binding forces), **not** a
`value.rs` sentinel change.  It is deep and byte-parity-risky (the guard
is `sui parity` staying at 28 match with `hello` byte-identical) and
warrants a deliberate design pass, not a rushed edit.

### Diagnostics available for the next attempt

- `SUI_M26_SELTRACE=1` — logs the exact demanded attrpath at each
  re-entrance (pins the frontier).
- `SUI_M26_NULLTRACE=1` — nix-trace of the sentinel softening path.

Both are default-off, zero hot-path cost, byte-parity-neutral (landed on
the `m26-fixpoint` / `m26-fixpoint-agent` branches; re-land when the fix
is attempted).

### REFINEMENT (session 3, 2026-07-11) — the load-bearing root is an OVER-FORCE, not the wrong-partial

The "publish per-key structure / field-lazy re-drive" framing above
patches the **symptom**.  A byte-verified discriminator pins the true
cause: sui forces a *sibling definition's* dynamic key that cppnix never
touches.

```
# modA: options.pleme.userName (str) ; options.homes (attrsOf int)
#       config.homes.${throw "KEYFORCE"} = 7
# modB: config.pleme.userName = "drzzln"
#   then select .config.pleme.userName
nix  → "drzzln"        (never forces the homes definition's key)
sui  → throws KEYFORCE  (OVER-FORCES it)
```

Demanding **only** `config.pleme.userName` (which depends solely on
modB's constant) must not force the `homes` definition at all.  sui does,
via `checkUnmatched → merged.unmatchedDefns → evalOptionValue homes →
dischargeProperties` (17-frame stack captured with `SUI_M26_TAILTRACE`).
That forced re-entry into the mid-force `config` is what yields the
empty-Promise partial → `null` → `homes.<null>` → `{}`.

**Consequence for the fix:** kill the over-force and the entire re-entry
cascade never happens — the field-lazy re-drive becomes unnecessary.  A
full mechanism-(a) `PromiseCell` re-drive prototype was built + measured
(parity-neutral; it *did* resolve `config.pleme` and advanced the repro
from `{}` to an `"expected list, got set"` `mergeDefinitions` frontier)
but did **not** close ROOT #2, and was reverted (never ship an
incomplete mechanism that patches the symptom).

### RESOLUTION (session 4, 2026-07-11) — ROOT #2 CLOSED at the exact force site

The over-force was **not** inside `evalOptionValue` / `mergeModules'`
(the interpreted lib executes byte-identically on both engines — a
`[EOV]` + `[PDP]` + `[PUSHDEF]`/`[RAWDEF]` trace on the patched
`modules.nix` showed the SAME force sequence up to the throw).  The
divergence is one level below the interpreter, in sui's own
`build_tail_attrs_now` (`sui-eval/src/eval.rs`).

Isolated, byte-verified repro (no module system at all):

```
# nix → "notype"   sui (before) → throws KEYFORCE   sui (after) → "notype"
let m = { config.homes.${throw "KEYFORCE"} = 7; }; in (m.config._type or "notype")
```

The ROOT #1 fix deferred a dynamic *tail* into a thunk, but that thunk,
when forced, **recursed the whole remaining tail eagerly**
(`build_tail_attrs_now(&tail[1..], …)`).  For a MULTI-level tail
(`config.homes.${e}`), forcing the head `.config` to WHNF therefore ran
`homes` *and* `${e}` in one shot — over-forcing `${e}`.  The module
system hits this because its definition collection reads
`m.config`'s WHNF (`pushDownProperties m.config` → `cfg._type`), which
in sui dragged the whole tail down to `${cfg.pleme.userName}`.

**The fix:** `build_tail_attrs_now` resolves EXACTLY ONE tail level and
re-defers the rest (`if tail.len() == 1 { leaf } else {
build_deferred_tail_attr(&tail[1..], …) }`) — cppnix's per-level lazy
nested-attrset-literal desugaring.  So forcing `.config` yields
`{ homes = <deferred {${e}=v}> }` and stops; `${e}` only resolves when
`.config.homes` is demanded.

Verification (all green): minimal repro → `{"drzzln":7}`; KEYFORCE
discriminator → `"drzzln"`; `sui parity` = **33 match · 0 tracked · 0
regressions** (`hello` drvPath `8550ijfw8imvqnky30gwdvwlidq3g42b-hello-
2.12.2.drv`, byte-identical to before); `sui-eval` lib 1314 passed;
`diff_eval_nixpkgs_lib` + `drv_path_parity` + `vs_cppnix` +
`construction_gates` + `property_eval` all green.  Sealed as the
`nested-tail-dynamic` `AttrKeyForceSite` (`laziness` domain) + three
corpus rows + two `diff_eval_nixpkgs_lib` regression tests.  The
byte-neutral `SUI_M26_SELTRACE` / `SUI_M26_TAILTRACE` diagnostics are
re-landed alongside the fix (default-off, zero hot-path cost).

`nixos_system_empty_modules_terminates` stays `#[ignore]`: the full
`lib.nixosSystem { modules = []; }` now advances past ROOT #2 to a
distinct `TypeMismatch { expected: "set", got: "null" }` frontier — a
new root, not this one.

---

**The surgical next step (as of session 3 — resolved above):** bisect
the over-force *inside* `evalOptionValue` / `mergeModules'` — find where
sui forces a matched definition's attrpath key (the `homes` def) while
resolving an unrelated field (`pleme.userName`), and make that
resolution demand only the option being selected, as cppnix does.
Diagnostics for it: `SUI_M26_SELTRACE` (select Missing→null softening) +
`SUI_M26_TAILTRACE` (deferred dynamic-tail-key resolution + force-stack
dump — the tool that pinned this root) live on branch
`m26-root2-overforce-diag` (commit `00ef8f2`, byte-neutral).  Byte-parity
held at **30 match · `hello` byte-identical** through every session-3
experiment.  *(Session 4 note: the bisect landed one level BELOW the
interpreter — in `build_tail_attrs_now`, not `evalOptionValue` — see the
RESOLUTION above.)*

---

## FIELD-LAZY ATTEMPT (session 5, 2026-07-11) — the `config` re-drive WORKS; the wall is the MUTUAL `config↔options↔merged` fixpoint

With the eager-attrpath-key family (roots #1–#3) fully sealed + landed,
ROOT #4 is the genuine field-independence gap.  A field-lazy
`ThunkRepr::Promise` re-drive was built and **byte-verified** (gated behind
`SUI_M26_FIELDLAZY`, default-OFF; `sui parity` = 35 match, `hello`
byte-identical, both OFF and ON; 1319 lib tests green).  Two proven facts:

1. **The `config` re-drive resolves.** A pure helper
   `eval::eval_letin_binding_whnf(config_body, env, "declaredConfig")`
   rebuilds only the `config` LetIn scope and forces `declaredConfig`,
   bypassing the `if`-body (which collapses to `{}` when re-driven whole
   because it re-enters `config` internally).  Trace:
   `result=Ok type=set nkeys=53 has_services=true` — **field-independent
   resolution of `config` is real and works.**

2. **The wall is the MUTUAL fixpoint, not `config` alone.** The same
   `config` thunk re-drives **5× per eval; the first 4 return `{}`, only
   the 5th is keyed.**  `declaredConfig = mapAttrsRecursiveCond … options`
   needs `options` at WHNF, but `options`/`merged` re-enter `config`
   through the `_module.args` bootstrap, so early-phase `options` is itself
   an empty partial.  borgbackup's `config.services` demand lands during an
   **early empty re-drive**, before the keyed one exists — so a
   `config`-only re-drive (or a per-thunk keyed memo) does not help the
   demanding site.  Forcing `options` first collapses the same way.

**The genuine fix (named, not yet built):** per-key field-laziness across
**all three** mutually-recursive fixpoint thunks — `config ↔ options ↔
merged` — so each field resolves against its own option definitions,
independent of sibling fields AND of the fixpoint phase.  Materially larger
than a single-thunk re-drive; a deliberate core-evaluator design pass
(candidate for `/twin-reasoning`).  The `in_promise_eval` null-softening
band-aid **cannot be removed** until it lands (still load-bearing for the
early-phase empties).  The proven `declaredConfig` resolver + the gated-OFF
machinery (`PROMISE_BODIES`/`FORCING_BODIES`/`PROMISE_REDRIVING`/
`CONFIG_KEYED` + `eval_letin_binding_whnf`) live on branch
`m26-field-lazy-fixpoint` (commit `5d15621`) for the next attempt to build
on — byte-neutral, zero default-path cost.

### DEFINITIVE PIN (session 6, 2026-07-11) — the re-drive family is DEAD; the fix is incremental WHNF key-publication

The mutual-fixpoint field-lazy re-drive was extended to per-key
(`DEMANDED_KEY` thread-local threaded through `eval_select`/`traverse_attrpath`
+ `eval_letin_binding_key_whnf` + demand-aware `CONFIG_KEYED` memo, gated
`SUI_M26_FIELDLAZY`, byte-verified 35 match both directions). It did NOT
close, and it proved **the entire re-drive approach cannot**:

- **The load-bearing re-entry is `config._module`** (the `_module.args`
  bootstrap demanded *during module collection*), NOT `config.services`.
  The byte-verified circular chain: `config → declaredConfig → options →
  merged → modules (doCollect) → applyModuleArgs → config._module.args.<name>
  → config`.
- **Why any re-drive fails:** `eval_letin_binding_key_whnf` rebuilds only
  the inner `config` let-scope, but its body
  `declaredConfig = mapAttrsRecursiveCond … options` references
  `options`/`merged`/`modules` as **free variables bound to the same
  mid-force thunks**, so forcing any key re-forces the *collapsed* `options`.
  Confirmed: `modules` (`doCollect`) re-drives to a real `list` in
  isolation, but `declsByName` (`zipAttrsWith`) → `Err(expected list, got
  set)` and `matchedOptions`/`resultsByName` → `nkeys=0` because their
  bodies read the mid-force `modules`/`config`.

**THE GENUINE FIX (definitive, after 8 agents):** not a re-drive — an
evaluator **laziness-model change**. A computed recursive binding
(`config`/`options`/`merged`) must **publish its result attrset's per-key
structure (keys as independent thunks) into the fixpoint `Promise` cell as
it reaches WHNF, before the strict downstream re-enters** — matching
cppnix, where option **key-structure** comes from *declarations*
(config-independent: `nix eval` gives `attrNames sys.options` = 53 /
`sys.options.services` = 1404 keys instantly) and only option **values**
depend on `config`. Separating key-structure from values across the mutual
fixpoint is the whole fix; it is the deepest core-evaluator change in the
plan and a **`/twin-reasoning` design pass**, not another agent peel. The
demanded-key plumbing (proven, byte-neutral, gated OFF) lives on branch
`m26-mutual-fixpoint` for that pass to build on. `nixos_system_empty_
modules_terminates` stays `#[ignore]`; no ROOT #5 (the eager-key family is
exhausted; this one root is the whole remaining gate).

### SHARPER PIN (session 7, 2026-07-11) — target is `options`/`merged`, not `config`; cheap repro; fix named to the line

The session-6 pin ("publish `config`'s per-key WHNF structure") was
directionally right but **mis-targeted** — byte-verified: publishing
`config`'s keys can't help because `config`'s key-SET comes from the
collapsed `options`.  The crash is one level upstream, in building
`options`.  Parity held 35 match / `hello` byte-identical both directions.

- **Cheap discriminator (use this, not the full `nixosSystem` build):**
  `attrNames sys.options` → **nix 53, sui `concatLists: expected list, got
  null`** — the option key-SET alone reproduces the whole gate, in a
  seconds-fast loop.
- **The detonator, one line:** `result = { options = checked options; … };
  checked = seq checkUnmatched;` (`modules.nix:358-362`).  Every
  `sys.options` access forces `checkUnmatched`, which forces
  `merged.unmatchedDefns`.
- **The two-stage cascade:** Stage A — `borgbackup`/`pki` read
  `config.services`/`config.certs` **during collection** → the
  `in_promise_eval()` null-softening at `eval.rs:1633`+`1669` returns
  `null`; Stage B — `unmatchedDefns`'s `concatLists` at `modules.nix:894`
  detonates on that `null`.
- **`_module.args` base keys ARE config-independently resolvable** (minimal
  repros pass); the bootstrap only detonates at full scale as a victim of
  Stage A.

**THE GENUINE FIX (named, byte-parity-risky, not yet built):** a
**declaration-vs-value split in `mergeModules`' collection fold** — collect
option *declarations* + *imports* to WHNF key-structure **without forcing
any module's `config` *value***, so the Stage-A/B class is unreachable and
the `in_promise_eval` null-softening can be removed.  The one remaining
isolation step before editing: a `SUI_M26_COLLECTTRACE` (log every
`config`-typed force while a `doCollect`/`mergeModules` frame is on the
force stack — the sibling of the `SUI_M26_TAILTRACE` that pinned ROOT #2)
to fix the exact over-force op to file:line.  Design + gated-OFF plumbing on
branch `m26-whnf-keypub`.

---

## SESSION 8 CLOSURE (2026-07-11) — M2.6 CLOSED via TWO targeted over-forces; the deep re-drive was NOT needed

The session-7 SHARPER PIN framed the gate as a "declaration-vs-value split in
`mergeModules`' collection fold, so the `in_promise_eval` null-softening can be
removed."  A `SUI_M26_COLLECTTRACE` isolation pass (per the session-7 plan)
plus a decisive cppnix discriminator **overturned that framing** — the
softenings during collection are NOT avoidable by a declaration-only split
(cppnix forces those `config.<field>` values too, it just resolves them
correctly).  The actual cause was **two eager over-forces of the ROOT #2/#3
family** — each isolatable to a **milliseconds-fast pure repro with NO module
system** — fixed at their load-bearing cause.  Once both landed, the softening
was **verified unnecessary and REMOVED**.

### ROOT #4a — `with X; body` eagerly EVALUATED the namespace (`eval.rs`)

```
# nix → [ "a" ]   sui (before) → throws "X"   sui (after) → [ "a" ]
builtins.attrNames (with (throw "X"); { a = 1; })
```

The `With` arm of `eval_expr` did `let scope_val = eval_expr(&ns, env)?;` —
its own comment claimed "don't force the namespace yet" but `eval_expr`
**evaluates** it.  For `with (throw …)` that runs the throw; for nixpkgs'
pervasive `config = mkIf … (with config.services.X; { … })` module shape it
forced `config.services.X` the instant the `with`-body's WHNF/keys were
demanded during collection (`pushDownProperties`), re-entering the mid-force
`config` fixpoint → the empty-Promise partial → the null softening →
`concatLists null`.  cppnix stores the namespace as a **thunk** and forces it
ONLY on a bare-ident lookup fallthrough.  **Fix:** `let scope_val =
maybe_thunk(&ns, env, false, None);` — the scope-lookup path
(`Env::lookup_fast`) already forces a `Value::Thunk` scope value on
fallthrough, so this is exactly cppnix's semantics; the fast-path for an
already-resolved ident namespace is preserved.  This alone eliminated the
`concatLists null` and advanced the frontier from collection all the way into
option matching.

### ROOT #4b — depth-≥2 dotted full-set leaf dropped on a Thunk-vs-Attrs merge (`eval.rs`)

```
# nix → [ "x" "y" ]   sui (before) → [ "y" ]   sui (after) → [ "x" "y" ]
builtins.attrNames { o.a = { x = 1; }; o.a.y = 2; }.o.a       # (and the reverse ordering)
```

nixpkgs' alsa module declares `options.hardware.alsa = { enable = …;
cardAliases = …; … }` (depth-2 dotted path, full-set leaf) AND
`options.hardware.alsa.enablePersistence = …` (deeper dotted path) in ONE
module; these must deep-merge.  `merge_nested_insert`'s `should_merge`
required BOTH collision sides to be concrete `Value::Attrs`, but a full-set
leaf is a lazy `Thunk` (attrset literals go through `maybe_thunk`), so the
Thunk-vs-Attrs collision fell to the OVERWRITE path — dropping the earlier
leaf's keys.  Result: `config.hardware.alsa` had only `{enablePersistence}`,
`cardAliases` "did not exist", `checkUnmatched` fired spuriously, and its
error-renderer's `cfg.enable` read hit the partial → the softening masked it
as the `condition = null` symptom.  **Fix:** `merge_nested_insert` forces each
side's thunk to WHNF **on collision only** (forcing an attrset to WHNF does
NOT force its fields — leaf laziness preserved), handling BOTH orderings
(existing-thunk / new-thunk); a thunk that forces to a non-attrset or errors
falls back to overwrite (leaf last-write-wins).

### Softening REMOVED — the band-aid is retired

With #4a + #4b landed, `SUI_M26_NOSOFTEN=1` (a temporary diagnostic that
disabled the `in_promise_eval` clause) proved both `config.system.name` →
`"nixos"` and `attrNames sys.options` → 53 **still hold**, `sui parity`
stays 35 match, and 1324 lib tests pass.  So the always-on
`|| crate::value::in_promise_eval()` clause in `eval_select`'s `bridge_active`
was **removed**.  The two explicit operator-gated bridges
(`SUI_BLACKHOLE_AS_NULL` / `SUI_BLACKHOLE_AS_EMPTY_ATTRS`) stay as opt-in
experiments; only the always-on Promise softening is retired.  (The session-6/7
gated-OFF field-lazy / demanded-key plumbing on branch `m26-whnf-keypub` was
NOT needed and is NOT part of this fix — the eval.rs/value.rs diff is only the
two laziness fixes + the softening removal + tests.)

### Verification (byte-parity SACRED — held throughout)

| criterion | nix | sui |
|---|---|---|
| `attrNames sys.options` (length) | 53 | **53** |
| `config.system.name` (`--json`) | `"nixos"` | **`"nixos"`** |
| `hello` drvPath | `j8q5j0x495…-hello-2.12.2.drv` | **identical** |
| `sui parity` | — | **38 match · 0 tracked · 0 regressions** (35 baseline + 3 new ROOT-#4 rows) |

Tests: 1324 sui-eval lib · 8 system_eval_parity (incl. the **un-ignored**
`nixos_system_empty_modules_terminates`, now HOME-relative + genuinely
passing) · 30 diff_eval_nixpkgs_lib · 23 drv_path_parity · 6 vs_cppnix · 22
construction_gates · 4 property_eval · 457 sui-spec — all green.  (`b77b3de`,
the test's pinned rev, also verified → `"nixos"`.)

Regression guards sealed: `with_namespace_is_lazy_on_body_whnf`,
`with_namespace_forces_only_on_fallthrough`,
`dotted_fullset_leaf_deep_merges_with_deeper_sibling`,
`dotted_fullset_leaf_deep_merge_reverse_order`,
`dotted_fullset_leaf_merge_preserves_leaf_laziness` (pure-unit in
`sui-eval::eval::tests`) + `module_with_namespace_config_read_stays_lazy`,
`module_options_fullset_and_dotted_sibling_deep_merge` (module-shape
diffs in `diff_eval_nixpkgs_lib`) + the un-ignored
`nixos_system_empty_modules_terminates`.

### Known residual (out of M2.6 scope, pre-existing)

- `sui eval --raw` on a string value emits it WITH quotes (`"nixos"`) where
  cppnix strips them (`nixos`).  A CLI **output-formatting** quirk affecting
  ANY string, NOT an eval divergence — the VALUE is byte-identical (`--json`
  proves `"nixos" == "nixos"`).  Independent of M2.6.
- ~~`diff_eval_primitives::diff_filter_attrs` fails identically on baseline
  `main` (nix 2.34 has no `builtins.filterAttrs`) — a pre-existing
  test-vs-nix-version mismatch, not a regression.~~
  **CORRECTED + RESOLVED 2026-08-17. The diagnosis above was wrong, and the
  wrongness is why this sat for months.** It was not a *version* mismatch:
  **no** CppNix has ever had `builtins.filterAttrs`, at any version or feature
  level (re-measured against nix 2.31.5 with `--extra-experimental-features
  'fetch-closure dynamic-derivations flakes nix-command'` — absent). The
  failure was sui exposing a nixpkgs `lib` function as a builtin, i.e. sui
  ACCEPTING a program nix rejects. Filing it as "the oracle's nix is the wrong
  version" made a real correctness defect read as test-harness noise, and
  because the test also sat behind `SUI_TEST_ONLINE` (which no workflow set) it
  reported `ok` while executing nothing — two independent reasons nobody looked.
  `filterAttrs` and five sibling leaks (`concatStrings`, `hasPrefix`,
  `hasSuffix`, `toLower`, `toUpper`) were removed from all three engines on
  2026-08-17. The test is now `diff_filter_attrs_is_rejected_by_both` and runs
  in CI's online step.
  **Generalise:** "our oracle is the wrong version" is the most comfortable
  explanation available for a differential failure and needs the same evidence
  as any other — one `builtins ? <name>` probe against the oracle would have
  refuted it here in seconds.
